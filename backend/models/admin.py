# This file intentionally left as a placeholder to avoid duplicate `Admin` models.
# Use `from models.administrateur import Administrateur` instead.
# The original legacy `admin` model was removed to keep the codebase consistent.
